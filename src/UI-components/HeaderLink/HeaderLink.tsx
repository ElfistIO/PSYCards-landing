import { ReactNode } from "react";

export type Props = {
  text: string;
  icon?: ReactNode;
  url?: string;
};

export const HeaderLink = ({ text, icon, url }: Props) => {
  return <div>aa</div>;
};
